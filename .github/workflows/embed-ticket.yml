- name: Generate embedding
  env:
    OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
    TICKET_ID: ${{ github.event.inputs.ticket_id }}
    TICKET_SUMMARY: ${{ github.event.inputs.ticket_summary }}
  run: python main.py
